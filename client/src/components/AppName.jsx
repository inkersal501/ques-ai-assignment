function AppName({color="#fff"}) {
  return (
    <h1 className="text-white text-2xl" style={{color:`${color}`}}>
      <span className="font-bold">Ques.</span>AI
    </h1>
  );
}

export default AppName;
