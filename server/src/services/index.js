const userService = require("./user.services");
const projectService = require("./project.service");
const transcriptService = require("./transcript.service");

module.exports = { userService, projectService, transcriptService };